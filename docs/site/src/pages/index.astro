---
import DocsLayout from '../layouts/DocsLayout.astro';
import fs from 'node:fs';
const read = (p) => fs.readFileSync(new URL(`../../../../${p}`, import.meta.url), 'utf8');
const intro = read('docs/README.md');
---
<DocsLayout title="CyberPanel AI Wizard Docs">
  <h2>Welcome</h2>
  <p>This site is generated from the markdown files in the repository's <code>docs/</code> directory.</p>
  <p>Browse a section using the navigation bar or start with <a href="/Cyberpanel-Server-Wizard-Cleanup/installation">Installation</a>.</p>
  <hr />
  <h3>Repository Index (Excerpt)</h3>
  <pre style="white-space:pre-wrap;font-size:0.8rem;">{intro}</pre>
</DocsLayout>
